import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function DatabasePage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Base de Datos</CardTitle>
            </CardHeader>
            <CardContent>
                <p className="text-base md:text-lg">Próximamente: Herramientas para gestionar miembros, inventario, cuotas y más.</p>
            </CardContent>
        </Card>
    );
}
